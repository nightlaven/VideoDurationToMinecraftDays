<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VideoDurationToMinecraftDays.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VideoDurationToMinecraftDays"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VideoDurationToMinecraftDays">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop></DesktopAcrylicBackdrop>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar x:Name="AppTitleBar"
       Title="Video Duration To Minecraft Days"
            IsBackButtonVisible="True"
            IsBackButtonEnabled="{x:Bind rootFrame.CanGoBack, Mode=OneWay}"
            BackRequested="AppTitleBar_BackRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/appicon.ico"/>
            </TitleBar.IconSource>
        </TitleBar>
        <Frame x:Name="rootFrame" Grid.Row="1"
         SourcePageType="local:ConverterPage"/>
    </Grid>
</Window>
